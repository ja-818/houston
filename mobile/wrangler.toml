name = "houston-companion"
# Deploy: cd mobile && pnpm build && npx wrangler pages deploy dist --project-name houston-companion
# Custom domain: companion.gethouston.ai (set in Cloudflare dashboard)

# Headers for PWA + caching
# [site.headers]
# "/*" = { "Cache-Control" = "public, max-age=3600" }
# "/sw.js" = { "Cache-Control" = "no-cache" }
